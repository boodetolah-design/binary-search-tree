# binary-search-tree

## Features

* Faster response than normal linear search
* Recursive binary search algorithm
* Dynamic memory allocation using malloc

## Prerequisites

* C Standard Libraries (`stdio.h`, `stdlib.h`) and `cs50.h`
* GCC or Clang compiler

## Building and Running

1. **Compile:**
   ```bash
   make treedata
